<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pint_android_v3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".quem_vai_consigo"/>
        <activity android:name=".viagens_marcadas" />
        <activity android:name=".viagens_efetuadas" />
        <activity android:name=".perfil_motorista_edit" />
        <activity android:name=".perfil_motorista" />
        <activity android:name=".desambiguacao" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu_municipe" />
        <activity android:name=".menu_motorista" />
    </application>

</manifest>